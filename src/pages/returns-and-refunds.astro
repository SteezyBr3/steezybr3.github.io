---
import Layout from "../layouts/Layout.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import ScrollReveal from "../components/ScrollReveal.tsx";
import { SITE } from "../config/site.mjs";
---

<Layout
  title={`Returns and Refunds | ${SITE.title}`}
  description="Information about returns, refunds, and order support."
  canonicalURL={`${SITE.url}/returns-and-refunds`}
>
  <Header />
  <main id="main-content" role="main">
    <section class="max-w-4xl mx-auto px-4 py-20">
      <ScrollReveal client:idle>
        <h1 class="text-5xl font-bold mb-6" style="color: var(--color-text);">Returns and Refunds</h1>
      </ScrollReveal>
      <ScrollReveal client:load delay={0.1}>
        <div class="prose prose-lg max-w-none" style="color: var(--color-text-soft);">
          <p>
            Add your returns and refunds policy here. You can explain your process for order issues,
            damaged items, notice periods, and how customers should contact you for support.
          </p>
          <p>
            This page is intentionally simple so you can replace the placeholder text with your own final policy.
          </p>
        </div>
      </ScrollReveal>
    </section>
  </main>
  <Footer />
</Layout>
